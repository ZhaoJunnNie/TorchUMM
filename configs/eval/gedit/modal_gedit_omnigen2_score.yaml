eval:
  benchmark: gedit

gedit:
  data_root: /datasets/gedit/GEdit-Bench
  out_dir: /outputs/gedit/omnigen2
  mode: score
  model_name: omnigen2
  instruction_language: all
  task_type: all
  run_calculate: true
  score_output_path: /outputs/gedit/omnigen2/summary.json
  vlm_eval:
    backbone: qwen25vl
    model_path: /model_cache/evaluator/Qwen2.5-VL-72B-Instruct

  score_model: wise
  score_gpu: "A100-80GB:3"
inference:
  backbone: omnigen2
  backbone_cfg: {}
