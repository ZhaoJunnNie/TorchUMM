eval:
  benchmark: gedit

gedit:
  data_root: ${UMM_DATASETS}/gedit/GEdit-Bench
  out_dir: output/gedit/bagel_sft
  mode: score
  model_name: bagel
  instruction_language: all
  task_type: all
  run_calculate: true
  score_output_path: output/gedit/bagel_sft/summary.json
  vlm_eval:
    backbone: qwen25vl
    model_path: ${UMM_MODEL_CACHE}/evaluator/Qwen2.5-VL-72B-Instruct

inference:
  backbone: bagel
  backbone_cfg: {}
  request:
    params: {}
