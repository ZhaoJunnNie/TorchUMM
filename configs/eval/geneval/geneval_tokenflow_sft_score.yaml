eval:
  benchmark: geneval

geneval:
  geneval_root: ${UMM_CODEBASE}/model/geneval
  metadata_path: ${UMM_CODEBASE}/model/geneval/prompts/evaluation_metadata.jsonl
  out_dir: output/geneval/tokenflow_sft
  images_per_prompt: 4
  max_samples: 0
  resume: true
  score_output_path: output/geneval/tokenflow_sft/summary.json
  mode: score  # full | generate | score
  vlm_eval:
    enabled: false
    max_new_tokens: 512
    max_retries: 2
  detection_eval:
    enabled: true
    eval_python: /ocean/projects/cis220039p/zzhou16/miniconda3/envs/umm_geneval/bin/python
    model_path: ${UMM_MODEL_CACHE}/evaluator
  run_scoring: true

inference:
  backbone: tokenflow
  backbone_cfg: {}
